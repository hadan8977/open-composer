interface:
  display_name: "Impeccable"
  short_description: "Design direction, references, execution, and critique"
  default_prompt: "Use $impeccable to guide this interface from goals and visual references through design decisions, implementation, and focused review."
