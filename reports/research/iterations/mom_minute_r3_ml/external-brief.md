# External Brief: mom_minute_r3_ml

This is a post-hoc remediation dossier, not evidence that the iteration gate was
passed before training. The source set supports a narrow meta-label experiment:
lagged momentum, volatility, and gap context may help accept or skip a frozen
rule entry, but cannot create entries or control orders. Alpaca IEX history is
research-only, costs must match the reference engine, and validation failure
must stop access to the reserved chronological holdout.

The corrected decision is to keep both trained families as diagnostic forward
shadow models only. A previous implementation opened the holdout after each
family won only one of three validation folds. Those holdout results are
invalidated and must never be described as independent evidence.
