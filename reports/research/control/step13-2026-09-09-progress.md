# Step 13 progress (Track L executor; one line per completed item: time, commit, result)

Plan: `docs/plan-step-13-recent-high-return-ml-and-llm-tracks-2026-09-09.zh.md`.
Gates: `config/promotion/recent-regime-high-return-gates-v2.json`.
Full narrative for the Step 11 Wave B closeout items below (placebo leakage
investigation) lives in `reports/research/control/step11-2026-09-06-progress.md`;
this file is the lighter, append-only log the plan's section 6 asks for.

- 2026-09-09 08:58 UTC -- `608309f` -- `--placebo-only` generalized with `--placebo-feature-set`, so both daily_only and daily_plus_intraday can be placebo-checked.
- 2026-09-09 08:58 UTC -- `cbba147` -- fixed `_run_label_shuffle_placebo` missing `extra_train_columns` + only shuffling one of three label columns (crashed on first real invocation this round).
- 2026-09-09 09:00 UTC -- daily_only placebo (post-cbba147, pre-bb21f27): selected cell IC 0.171 (all 6 cells 0.05-0.17) -- still far above the 0.02 clean threshold. Not yet root-caused at this point.
- 2026-09-09 09:07 UTC -- `000b59c` -- Track L L0 collector (`open_composer/research/news/collector.py` + `scripts/collect_alpaca_news_packets.py`), 9/9 tests, one-day smoke test (2024-06-03, 897 articles) passed against the real API.
- 2026-09-09 14:19 UTC -- `bb21f27` -- **root cause found and fixed**: `GridSelectedLightGBMStrategy.fit()` never held out the validation year from the fit pool (fit on the entire training window including the validation year, then scored that same year -- in-sample, not a real holdout). Confirmed by the placebo's own pattern (deeper trees showing 2-3x the shallow trees' spurious IC at the same horizon -- memorization capacity, not signal). Fix: fit_rows now excludes the validation year plus a `2 * label_horizon_days`-calendar-day embargo before it. New regression test reproduces the mechanism at synthetic scale. 43/43 kernel tests pass.
- 2026-09-09 14:19 UTC -- daily_only placebo rerun launched with the fix (`/tmp/placebo_daily_only_v3.log`) -- numbers pending, will update this line once landed.
- 2026-09-09 14:30 UTC -- (L-track executor handoff; recorded by pathspec commit, see next entry) -- `/tmp/placebo_daily_only_v3.log` landed: post-fix daily_only placebo mean rank IC **0.0013** (clean, threshold 0.02), selected cell `h21_d6`. Full three-number record now in `step11-w2-model-ranking-2026-09.md` sections 3-4: pre-fix daily_only 0.171, pre-fix daily_plus_intraday 0.206, post-fix daily_only 0.0013. daily_plus_intraday feature set stays **closed** (already worse than daily_only even under the shared pre-fix bias); only daily_only is queued for a clean re-run, test years 2024-2026 only, later when the box is free -- not run this session. Collector (`collect_alpaca_news_packets.py --start 2024-01-01`, PID 1939055/1939060) confirmed still running throughout, not restarted.

## Track L executor log

- 2026-09-10 08:5x UTC -- 4.2 extraction module landed: `open_composer/research/news/extract.py` (prompt+schema+EVENT_TYPES fixed in code per plan, batches of 25, cache by `(id, prompt_hash)`, `data/features/news_events/{year}.parquet` output, `compute_weekly_candidate_pools` for the top-150-by-`momentum_252_21` scope -- reuses `universe.py::load_universe_panel`/`exclude_funds_and_etfs` and `loop.py::universe_as_of_calendar_month` rather than reimplementing PIT cohort lookup) + `scripts/extract_news_events_llm.py` (`--smoke-n`, bulk `--start/--end` with budget gate and auto-capped `--end` against the latest collected day). Cross-boundary edit (not an L-owned file, plan section 6 requires disclosure here): `open_composer/research/llm_backends.py::OpenAIBackend` got one additive method, `infer_with_usage()`, next to the existing `infer()` (unchanged behavior/signature, verified by the existing `pit_semantic_theme_forward.py`-facing tests still passing) -- needed real token counts for the extraction ledger's 30M-token budget, which `infer()` discards. 38/38 new+touched tests pass (`test_news_extract.py`, `test_openai_config.py`, `test_news_collector.py`), ruff clean.
- 2026-09-10 -- collector confirmed finished (per coordinator): `data/features/news_packets/{2024,2025,2026}.parquet` + 983-day `_daily/` archive present; not restarted, read directly from `_daily/` (the CLI's bulk path also auto-caps `--end` to the latest collected day as a safety net).

## blocked_on_user / cross-agent coordination notes

- Per the 2026-09-09 14:10 UTC coordinator message: a third agent now owns `panel.py`'s `extra_feature_roots` parameter (needed first for the open factor-library tables). L-track does **not** edit `panel.py`; will use `extra_feature_roots` once that agent lands it and the coordinator signals it's ready.
- The pre-fix daily_only/daily_plus_intraday B3 grid ledger entries (`step11_b3_lightgbm_grid_daily_only`, `step11_b3_lightgbm_grid_daily_plus_intraday`) were selected via the in-sample mechanism above and must be treated as unreliable until re-run with `bb21f27`'s fix -- this affects the Wave B report's section 2-3 verdicts and, per plan section 3.4/7, gates whether Track M may reuse the grid-selection path at all.

## Track M executor log (M-track executor; one line per completed item)

- 2026-09-09 -- `add74a6` -- loop.py Step 13 extensions (train_window_months, refit_frequency, recency_halflife_days, trend_gate, universe_top_n); B0-B3 config_hash reproducibility preserved by conditional hash-payload inclusion; 23 new tests.
- 2026-09-09 -- `d3f824e` -- `open_composer/research/regime/validated_grid_strategy.py`: leakage-safe out-of-sample validation-selected grid strategy (fit/validation split with embargo, built independently of but consistent with A-group's `bb21f27` fix); `scripts/build_step13_regime_daily_features.py` run for real (2016-2026, 2684 rows, real CBOE VIX at 100% coverage, no fallback needed).
- 2026-09-10 -- `ea6eb15` -- `evaluate_recent_high_return_candidate` (v2 contract gate evaluator) added to `regime/gates.py`; v2 contract JSON's missing required `rationale` field added (structural fix, zero threshold changes, no ledger entries existed under this family before this commit); `scripts/run_step13_m_grid.py` M0 driver written (6 static rule cells, checkpointed via ledger config_hash skip, `feature_set` resolved through a registry not hard-coded).
- 2026-09-10 01:10 UTC -- M0 launched detached (`nohup ./scripts/run_capped.sh --mem 1.8G -- uv run python scripts/run_step13_m_grid.py --stage m0 > /tmp/step13_m0_run.log 2>&1 &`); first attempt crashed on a trading-calendar/regime-table year mismatch (BIL's full 2016-2026 history leaking pre-2022 dates into the weekly rebalance schedule), fixed by scoping the BIL augmentation to the same `years` the equity panel uses; relaunched and confirmed alive past the earlier crash point (loading price panel) before this turn ended. Awaiting exit notification -- see /tmp/step13_m0_run.log.

## Track F executor log (F-track executor; plan `docs/plan-step-13f-open-factor-library-import-and-screening-2026-09-09.zh.md`)

- 2026-09-09 14:55 UTC -- `8d94769` -- 3.0 dependency/data check: `py-alpha-lib` installs but its generated code needs Python >=3.12 (this venv is 3.11); `polars`' default wheel needs AVX2, this box's CPU has only baseline AVX (SIGILL on use). Both reverted with `uv remove`; `uv sync --extra workbench` restored the workbench-extra packages that `uv add`/`uv remove`'s default-group sync had dropped along the way (mlflow/quantstats/matplotlib/etc, verified re-importable). `data/sip/daily` confirmed `symbol, timestamp, open, high, low, close, volume, trade_count, vwap` (no `amount`); universe union 2,721 symbols. Fetched real yli188/WorldQuant_alpha101_code and Daic115/alpha191 source to cross-check formulas before independent pandas reimplementation (neither has a visible LICENSE; not vendored).
- 2026-09-09 15:34 UTC -- `7759e90` -- 3.1 Alpha158: `open_composer/research/features/alpha158.py` (154 cols: 9 K-bar + 29 rolling groups x {5,10,20,30,60}; documented why not the nominal 158), `scripts/build_alpha158_features.py` (per-year/per-300-symbol-batch, resumable), 7 tests (hand-rolled loop reference cross-check). Real build completed for all 11 archive years under `run_capped.sh --mem 1.8G` in the background: `data/features/alpha158/{2016..2026}.parquet`, 6,111,014 rows total, 156 columns, `MANIFEST.json` records per-year rows/build-seconds (data/features/ is gitignored -- code+tests committed, not data).
- 2026-09-10 01:20 UTC -- `38fa5d3` -- 3.2 Alpha101 (20/101 ids, Kakushadze Table 1 formulas needing only OHLCV+vwap) and Alpha191 (20/191 GTJA ids needing only OHLCV+vwap, no turnover/DECAYLINEAR) pandas fallback (py-alpha-lib unusable per 3.0), shared wide-panel operator library `_panel_ops.py`. Fixed 3 formula bugs found by comparing the fetched references against their own quoted formula text (alpha191 008/019/020/029) and one warm-up bug caught by the test suite (gtja003's first row fabricated 0 instead of NaN). 22 tests, all passing. `alpha101_skipped_ids()`/`alpha191_skipped_ids()` will feed the MANIFEST.json `skipped` lists once the real data build runs (not yet run -- next: 3.4 registry/panel.py first per coordinator priority override, since Track M needs `extra_feature_roots` + the alpha158 feature set today).
- 2026-09-10 01:31 UTC -- `85761b2` -- 3.4 `open_composer/research/features/feature_sets.py` (registry: `daily27`/`alpha158`/`alpha101`/`alpha191`/`all_open`/`screened_top40_recent`) + `panel.py::load_feature_panel(..., extra_feature_roots=())` (left join per root on symbol/trade_date, float32, NaN kept, only the requested columns pulled per root via a footer-only schema probe; 3 pre-existing tests untouched, 3 new tests added). Unblocks Track M today (`feature_set="alpha158"` usable now).
- 2026-09-10 01:47 UTC -- (no new commit; data/features/ is gitignored) -- `scripts/build_alpha101_alpha191_features.py` real build finished (launched detached via `nohup ./scripts/run_capped.sh --mem 1.8G -- ...`, log `/tmp/build_alpha101_alpha191.log`), verified from the files: `data/features/alpha101/` and `data/features/alpha191/` each hold `{2016..2026}.parquet` + `MANIFEST.json`, 13 entries each. Both manifests: `rows_by_year` identical between the two tables (as expected, same input), `row_count_total` 6,256,661, `column_count` 22 (symbol+trade_date+20), `skipped_count` 81 (alpha101) / 171 (alpha191) matching `101-20`/`191-20`. Caveat recorded, not chased further per the economy instruction: this total is ~145k rows higher than alpha158's 6,111,014 from the prior build (including in years like 2016 that cannot have grown from new trading days), most likely because `data/features/universe/` was extended by another track between the two build times (universe union is a full-history symbol set, so admitting one more symbol retroactively adds its rows in every year) -- both tables are internally consistent for their own build time; not re-verified against a byte-identical universe snapshot.
- 2026-09-10 02:12 UTC -- `a1a9c56` -- `scripts/build_alpha101_alpha191_features.py` had been written and used for the 3.2 real build above but never committed; committed now (code-only, matches the on-disk data it already produced).
- 2026-09-10 02:12 UTC -- `3d3feb2` -- 3.3 OSAP price-only table: `open_composer/research/features/osap_price.py` (24 CRSP price/volume-only OSAP signals as daily rolling proxies) + `scripts/build_osap_price_features.py` (per-year/per-300-symbol-batch, resumable, `LOOKBACK_YEARS=6` since `momseason_2_5` needs ~5.1y). Found and fixed two real bugs beyond the planned `_guard()` warm-up wiring while getting tests green: DuckDB's `LOG(x)` is base-10 not natural log (silently wrong `log_price`/`dolvol_63` vs `np.log`), and DuckDB raises on `LN(0)` (real SIP zero-close bad ticks) where plain division would have silently produced inf/nan instead -- fixed by nulling non-positive `close` at the source plus a `_safe_ln()` guard. 8/8 tests pass. Verified from disk after a full-universe (2,721 symbols) smoke build for 2026: `data/features/osap_price/2026.parquet` 464,431 rows x 27 columns, `MANIFEST.json` `row_count_total` 464431, `column_count` 27, `skipped_count` 1 (`betatail_252`, plan-optional). Remaining 2016-2025 years launched detached next; will re-verify full row/column counts once that process exits.
- 2026-09-10 02:45 UTC -- OSAP full 2016-2025 build finished (detached, `/tmp/build_osap_price.log`). Verified from files (pyarrow metadata, not just the log/manifest self-report): all 11 `data/features/osap_price/{2016..2026}.parquet`, 27 columns each, row counts sum to exactly `MANIFEST.json`'s `row_count_total` 6,113,727.
- 2026-09-10 08:05 UTC -- `29b41e3` -- Step 13-P scope addition A1: `open_composer/research/features/reversal_trend_daily.py` (25-col `data/features/reversal_trend/{year}.parquet`, float32) reuses Track P's `open_composer/research/pine_port/reversal_trend.py::compute_reversal_trend` (found already written on disk, uncommitted at the time -- Track P was mid-commit; verified it exports `compute_reversal_trend` and is complete before reusing, per the coordinator's "reuse if landed" instruction) rather than reimplementing the state machine. Adds only the derived columns the daily table needs beyond that port's own output (macd_hist_slope, atr_ext_ema20, 3 EMA/price ratios, 4 "bars since last True" counters, symbol-grouped). Documented judgment call: the 4 discrete `rt_*_signal` columns are event-study-only, `rt_os_active`/`rt_ob_active` (also 0/1 but not "signals" in the port module's own vocabulary) are treated as continuous and go to the 3.5 screen -- 21 continuous / 4 signal, `REVERSAL_TREND_CONTINUOUS_COLUMNS`/`REVERSAL_TREND_SIGNAL_COLUMNS` make this a checkable partition. `scripts/build_reversal_trend_features.py`, `LOOKBACK_YEARS=1` (recursive-filter indicators, no long rolling window unlike OSAP). 9 new tests (state-machine branch coverage itself stays Track P's own test file's job; mine cross-checks signal/bars-since columns against real multi-year SPY data). Smoke-tested full 2,721-symbol universe for 2026 before committing: 464,431 rows x 27 cols, 563/1499/943/1015 bull/bear/recL/recS events -- this file is the real 2026 output, survived the cap reset on disk.
- 2026-09-10 08:15 UTC -- `de83709` -- found `open_composer/research/features/alpha101.py` had an uncommitted one-line fix (`close.pct_change()` -> `close.pct_change(fill_method=None)`, pandas' deprecated default forward-fills across NaN gaps before differencing) sitting in the working tree since the original 3.2 session, never actually committed alongside `38fa5d3`. Confirmed it is mine (matches my own documented reasoning, tests green), committed standalone. **Caveat, not yet acted on**: the already-built `data/features/alpha101/{year}.parquet` (13 files, built under the old buggy default) may have silently-forward-filled `returns` on any (symbol, date) where that symbol's wide-panel close was NaN (new listings, data gaps) -- likely a small fraction of rows, not systematic, but not re-verified or rebuilt this turn (out of today's stated order, deadline-pressured); flagging for the coordinator to decide whether a rebuild is warranted before 3.5 screens alpha101 columns.
- 2026-09-10 08:22 UTC -- Reversal-trend full 2016-2025 build launched detached (`nohup ./scripts/run_capped.sh --mem 1.8G -- uv run python scripts/build_reversal_trend_features.py > /tmp/build_reversal_trend.log 2>&1 &`), confirmed alive past the 2016 batch-scan start before ending this turn (2026 will skip, already on disk). Next per coordinator: A2 event study once this table lands, remaining `feature_sets.py` entries (`osap_price`/`reversal_trend` continuous columns), then the 3.5 screen as a detached job -- deadline today 18:00 UTC.
- 2026-09-10 01:22 UTC -- `144c465`, `88c2f2a` -- M0 crashed twice more after the first fix: (1) regime_daily/equity-panel one-day lag on the newest date (2026-09-09), fixed by clipping to regime_daily's own max date; (2) TEST_YEARS wrongly included 2022-2023 as walk-forward test periods with no prior history to embargo against -- split into DATA_YEARS (2022-2026, panel load range) vs TEST_YEARS (2024-2026 only, the real gated window). Relaunched a third time (detached, `/tmp/step13_m0_run.log`); confirmed alive past both prior crash points before this turn ended. Awaiting coordinator wake-up notification.
- 2026-09-10 (M0 real results, not yet separately logged) -- all 6 M0 cells failed `all_gates_pass`; best cell `step13_m0_momentum_252_21_gate_off` cagr_recent_net=0.3321 mdd=-0.4721 hit_rate_weekly=0.5870 activity_floor_min=36 (clears CAGR/activity/DSR/stress-cost, fails `cagr_excess_vol_matched_spy` -0.34 and `max_drawdown_recent` badly); `ret_126_rel`/`ret_63_rel` cells near-zero-to-negative with -52% to -65% MDD (small/micro-cap dilution per coordinator, not a bug). Recorded in the v2 ledger.
- 2026-09-10 -- `08d8e24` -- fixed a stale assertion in `test_regime_gates_v2.py`: `_fixture()` deliberately carries 2021-2023 data for the disclosure block/2022 replay, but the slicing test wrongly asserted `cagr_2018_2023 is None` with a copy-pasted comment claiming no pre-2024 data exists. `gates.py` itself was already correct; caught by re-running the full suite before touching the grid script again.
- 2026-09-10 -- `66208b3` -- coordinator relay: reconciled M0's momentum_252_21/top1500/top20 cell against the coordinator's independent close-to-close/no-cost check. `reconcile_m0_close_marked_no_cost()` (execution=close_marked, cost=0): cagr_recent=0.3526 mdd_recent=-0.4447 -- NOT within a few points of the coordinator's ~0.235. Root cause found by direct measurement: `data/features/universe/`'s monthly PIT cohorts only hold ~1150-1175 distinct symbols (sampled 2024-01/2025-06/2026-08), well under 1500, because `universe.py`'s own build already applies a $5-min-close + ADV-eligibility + top-1500 cap upstream -- so `universe_top_n=1500` was a silent no-op against this table, and M0's "top20-of-top1500" cell was really "top20-of-the-full-already-liquidity-prefiltered-~1150-1175-name-pool" (more large/liquid-tilted than a literal top-1500-of-the-~2700-symbol `data/features/daily` market), plausibly explaining why it runs hotter than the coordinator's raw check in a mega-cap-led rally window. M0b's 500/200 tiers are unaffected (both below the pool's real size, so they still bind correctly); flagged to the coordinator, not fixed here (the universe table's build cap is outside Track M's owned files).
- 2026-09-10 -- M0b preregistered (this line, written before launch per the mandatory rule): 16-cell grid, ledger family unchanged (`step13_recent_high_return`), DSR trial count grows honestly across M0+M0b. Grid: `universe_top_n` in {500, 200} x `score` in {`momentum_252_21`, `momentum_252_21_over_vol_63` (derived: momentum_252_21/vol_63, inf-from-zero-vol mapped to NaN)} x `top_k` in {20, 50} x `trend_gate` in {on, off} = 16 cells. Cell IDs: `step13_m0b_{mom|mom_over_vol63}_uni{500,200}_k{20,50}_gate_{on,off}`.
- 2026-09-10 -- `30879b4` -- coordinator bisection ("reconciliation not closed, universe theory is wrong -- bisect inside your machinery"): fed the coordinator's own 133 real weekly signals/picks (CSV dump, real prices, no costs, ground truth CAGR 0.235/MDD -0.436) directly through `returns_from_weight_schedule`, bypassing all selection/scheduling code. Found the real bug: the function recomputed every day's return with the *same*, never-updated target weight for the whole holding window -- mathematically daily-rebalance-to-constant-weight, not buy-and-hold. Pre-fix: 0.3213 CAGR on identical symbols/dates/prices (vs 0.235 truth). Fixed to accumulate cumulative gross return per held name since the window's start, weight once, sum, then pct_change (true buy-and-hold with weight drift); provably identical to the old formula when there's no return dispersion (new degenerate-case test), which is why no prior test caught it. `scripts/evaluate_cross_sectional_momentum_liquid500.py` (Step 10) has the same bug independently -- flagged, not changed (outside Track M's files). Full test suite: 18 pre-existing failures, all in other tracks' files, zero in kernel/regime/Track-M tests.
- 2026-09-10 -- residual gap after the fix, isolated: feeding the coordinator's exact picks through the fixed function gives 0.2927 (vs 0.235 truth) -- the remaining ~5.6pp is a *separate*, non-bug methodology difference: `returns_from_weight_schedule` holds until the *next scheduled rebalance event's date*, while the coordinator's `nxt_end` is "signal + more than 6 calendar days" -- these diverge on holiday-shortened weeks (7/132 in the sample). Confirmed by recomputing buy-and-hold with each boundary convention by hand: next-event-date boundary -> 0.2991 (matches the fixed function almost exactly); nxt_end boundary -> 0.2363 (matches the coordinator's 0.235 almost exactly). My own live pipeline (own schedule generation, not the frozen CSV) additionally has 139 rebalance dates vs the coordinator's 133, and differs by 1-5 names on 14/132 overlapping weeks (borderline universe-cohort tie-breaks) -- real, disclosed methodology differences, not bugs.
- 2026-09-10 -- ledger hygiene: purged the 6 M0 + 16 M0b entries computed under the pre-fix formula from `reports/research/ledger/experiments.jsonl` (gitignored/not git-tracked; backed up to `/tmp/experiments.jsonl.bak_pre_fix_purge` first; the other 10 entries from `step11_baseline_chain`/`groupb_recent_regime_high_hit_rate` were left untouched -- out of scope, flagged to the coordinator that they likely share this bug too since Step 10/11 share the same execution paths). Re-ran M0 (6 cells) and M0b (16 cells) under the fixed formula -- both still fail `all_gates_pass` (M0's best cell, momentum_252_21_gate_off, now 33.74% CAGR/-47.2% MDD, barely moved since this cell's own dispersion characteristics differ from the CSV's; M0b's least-bad cell, mom_over_vol63_uni500_k50_gate_on, 24.25%/-27.04%/hit 0.60/excess -0.164, still fails `cagr_excess_vol_matched_spy` like every other M0b cell). Full corrected tables sent to the coordinator directly (not duplicated here).
- 2026-09-10 -- `9f72123` -- generalized `stage_m1_single_cell` to take `universe_top_n`/`top_k`/`feature_set` as real parameters (CLI: `--universe-top-n`, `--top-k`, `--feature-set`), resolved through Track F's real `feature_sets.resolve_feature_set` registry (commit `85761b2`) instead of the local dead `FEATURE_SET_REGISTRY` (removed). Per coordinator: run on universe_top_n=500, top_k=50, gate on, first daily27 then alpha158.
- 2026-09-10 -- `558c60f` -- M1 single-cell's first-ever real invocation crashed immediately: `panel[REGIME_FEATURE_COLUMNS]` used a bare tuple as a pandas column indexer (asks for one column literally named by that tuple, MultiIndex-style) instead of `panel[list(REGIME_FEATURE_COLUMNS)]`. Fixed; relaunched detached (real cell then placebo, chained sequentially in one background job): `/tmp/step13_m1_single_cell_daily27_v2.log`.
- 2026-09-10 -- `3dc7850` -- per coordinator decision: kept the "hold until next scheduled rebalance event" calendar convention (matches what a live weekly strategy does; the coordinator's nxt_end rule was a quick check, not the target). Added a caveat paragraph to `step11-w2-model-ranking-2026-09.md` (top of section 2) and a one-line caveat to `step12-groupb-recent-regime-2026-09.md` (top of section 1): every return series in both reports predates `30879b4`, the bug only overstates returns (never understates), so no already-failing verdict can flip to pass; a clean re-run is deferred, and `evaluate_cross_sectional_momentum_liquid500.py`'s independent copy of the same defect is noted, not fixed. M1 single-cell (daily27, universe_top_n=500, top_k=50, gate on) + placebo still running (real LightGBM quarterly refits take longer than M0/M0b's rule cells); a watcher is polling `/tmp/step13_m1_single_cell_daily27_v3.log` for completion.
- 2026-09-10 -- daily27 M1 single-cell + placebo landed (per coordinator): real cell validation IC mean 0.063 (all 11 quarters positive), cagr_recent_net=3.5%, mdd=-18.5%, hit=0.548, vs rule baseline 33.7% (fails ml_must_beat_rule_baseline badly); placebo IC 0.0011 (clean, |IC|<0.02). `1d95989` -- added `TopNPreFilteredStrategy` (`regime/validated_grid_strategy.py`) for the two-stage cell: wraps an inner ranking strategy, restricts `.score()` to the top-N names by a rule score within the already-ADV-filtered universe (everyone else gets -inf so `nlargest(top_k)` can never select them), `.fit()` unchanged (trains on the full window, only scoring/selection is pre-filtered). 2 tests (stub-inner pool-scoping proof, fit-sees-full-frame proof).
- 2026-09-10 -- **two-stage cell preregistration (this line, written before launch)**: `step13_m1_two_stage_daily27_uni500_pf100_k50_gate_on` (+ its placebo) -- pre-filter each rebalance date to the top-100 of the top-500 (ADV) universe by `momentum_252_21/vol_63`, daily27+regime5 LightGBM model (`ValidationSelectedLightGBMStrategy`, single grid cell h5/d3, quarterly refit) ranks only those 100, hold the top 50, trend gate on. Rule twin (`ml_must_beat_rule_baseline` comparison) is M0b's own `step13_m0b_mom_over_vol63_uni500_k50_gate_on` (24.3% CAGR) -- same pre-filter score/universe/top_k, like-for-like ML-vs-rule comparison, not the looser "best M0/M0b cell so far" baseline. Ledger family unchanged (`step13_recent_high_return`).

## Track P executor log (P-track executor; plan `docs/plan-step-13p-reversal-trend-pine-factor-and-strategy-2026-09-09.zh.md`)

- 2026-09-10 (retroactive note) -- step 1 code is in HEAD as commit `0a6e8e4` ("docs: Step 13 Track M -- calendar-convention decision..."), not a Track P commit: a concurrent `git commit` without a `-- <pathspec>` restriction appears to have swept up my already-`git add`-staged files before my own `git commit -- <mypaths>` ran, which then found nothing left to commit ("no changes added to commit"). Confirmed by `git log -- open_composer/research/pine_port/reversal_trend.py` and `git show HEAD:...`; coordinator independently confirmed the same. Content unaffected, just cross-agent commit-message noise: `open_composer/research/pine_port/reversal_trend.py` (`compute_reversal_trend`, full port of `docs/inputs/pine/reversal_trend_0522.pine` per plan section 1 -- ta.ema/rma/rsi/atr/macd/dmi vectorized with pandas, arm/expire/lock/cooldown state machine run as an explicit per-symbol bar loop) + `tests/test_reversal_trend_port.py` (22 tests: EMA/RMA/RSI/ATR/MACD/ADX vs independent plain-Python reference implementations at rtol=1e-9 with exact warmup-length assertions, every state-machine branch hand-traced on synthetic boolean sequences, per-symbol independence, no-repaint on both synthetic data and a real SPY 2018-2025 daily window). Track F already reused `compute_reversal_trend` for A1 (see their `29b41e3` line above) -- its interface is now frozen per the coordinator's instruction, not touched since.
- 2026-09-10 -- `c6af78b` -- step 2: `scripts/reversal_trend_parity.py <symbol> <1h|1D> <start> <end>` prints f_bull/f_bear/f_recl/f_recs timestamps (+close/rsi/adx/ema context) with 500d (1D) / 120d (1h) of pre-`start` warmup history fetched so EMA200/ADX/RSI are converged like a real chart. 1h reads `data/bars/hourly/{year}.parquet` and raises a clear error since that archive doesn't exist yet (step 3, not run this turn); 1D reads the local SIP daily archive directly and was actually run. Ran for SPY and NVDA daily bars 2025-01-01..2025-06-30: **zero signals for both symbols in this window**, verified not a bug by direct inspection, not just assumed: SPY's RSI touched oversold twice (2025-03-11..13, 2025-04-03..08) but snapped back too fast for `dwell_os` to ever reach the required 5 bars (violent V-shaped moves, not the sustained-basing pattern `recL` wants), and the one MACD bullish crossover inside the active OS window (2025-04-22) fired while price was still below EMA20, so `bull` never triggered either; NVDA's RSI only briefly touched 28.5 once, dwell_os maxed at 1. Corroborated the pipeline is not systematically broken by also running the same params over 2018-01-01..2025-06-30: SPY 3 bull (2018-11-28, 2019-08-21, 2023-10-09) / 6 bear / 1 recL / 3 recS; NVDA 0 bull / 6 bear / 3 recL / 7 recS across 7.5 years -- NVDA never firing `f_bull` in the full sample is a genuine, disclosed characteristic (momentum stocks rarely give this indicator's specific pullback-then-MACD-confirm setup), not tuned or changed per plan section 6 ("不做：调指标参数"). Full timestamp lists sent to the coordinator directly, not duplicated here.
