# Paper Validation Remediation: 2026-07-09

- Status: blocked_pending_fill
- Failed step: `state_drift`
- Root cause: supervised alignment entered the authorized Alpaca Paper order path and submitted a `SELL TQQQ` order, but the broker still reports the order as open/accepted, so the account remains in TQQQ while the engine target is GLD.
- Broker evidence: order `ba264070-cb81-4cc2-a5e3-9f1d669eeb9a` / signal `sig_bec7527e6914e6da` is `OrderStatus.ACCEPTED` in `reports/paper/open_orders.json`; `reports/paper/reconciliation.json` reports `open_order_count=1` and `filled_order_count=0`.
- Action taken: reran `uv run python scripts/run_daily_paper_cycle.py` inside the Alpaca OPG submission window at 2026-07-09 19:06 EDT; readiness passed, router order authorization was active, and the cycle used `--allow-paper-orders`.
- Required next action: after the accepted order is filled or canceled by Alpaca Paper, rerun `uv run python scripts/run_daily_paper_cycle.py`, then rebuild `uv run oc paper validation-report`; proceed to Wave 9.1 only after the counted validation day has no `state_drift_warning`.
- Real-money status: live broker writes remain out of scope.
