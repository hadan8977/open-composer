# 周额度重置后的统一计划（2026-09-11 起草；用户 2026-09-11 已认可架构与优先级；周额度重置 = 北京时间周日 08:00 / UTC 周日 00:00）

作者：协调者（Fable）。执行者：额度重置后的执行代理（无上下文；每个工作流的细节见各自计划文件）。
本文件把 Step 13（M/F/P/L）、Step 14（通用运行器）、Step 15（GTJA-17 + 内部人）、Step 16（Sonnet 调研）合成一份有优先级的清单。

## 0. 前提（用户决定与纪律，2026-09-11）

- 门槛合同 v2 不变，`cagr_excess_vol_matched_spy` **不放宽**；自带杠杆的 ETF（TQQQ 类）允许作为标的，融资杠杆只作参考。
- 通用运行器而不是按周期各写一个（Step 14）；评估引擎同样按 `--timeframe` 参数化，周期集合预注册、DSR 按全部周期的格子计数。
- 按用户的**意图**思考而不是字面执行；每次汇报末尾固定"我接下来会做什么、为什么"。
- 架构假设（用户观点，与我们的证据一致）：纯价量信号做择时/执行层；选股层需要带信息的数据（内部人申报、空头利息、13F、新闻事件、财报）；ML/LLM 负责组合。
- Alpaca 行情用 SIP（用户已多次说明），实时小时线/分钟线信号与回测同源，不需要 IEX 差异披露。
- 额度纪律：最多两个执行代理同时活动、全部新代理、重计算一律后台分离带检查点、`run_capped.sh --mem 1.8G`。

## 1. 现有资产与证据（不用重做）

- 数据与特征：SIP 日线/分钟档案（2016→）、1h bar（2024→）、五个公开因子库表（alpha158/101/191/osap/reversal_trend）、
  新闻 PIT 包（2024→，685k 篇）、regime_daily。
- 机器：`scripts/screen_factors.py`（IC/ICIR/FDR/去重，带检查点）、`scripts/run_step13_m_grid.py`（规则与 ML 单元，账本去重）、
  `open_composer/research/regime/reversal_trend_hourly.py`（逐笔引擎）、`scripts/extract_news_events_llm.py`（LLM 事件抽取，端点暂停）。
- 产品：`us_recent_high_return_top50` 观察模式已接入（23:15 UTC cron），规则路径支持比值评分与趋势门。
- 证据（2024-01→，周频，10 bp）：规则动量 33.7%/-28%；所有 ML 单元 4-20%，验证 IC ≤ 0.035；小时线 Reversal Trend 最佳 26%/-15%（网格边缘）；
  没有任何单元通过合同 v2；SPMO 37%/-20% 是诚实的对照。
- 文献（来源卡 step13f/step14/step15/step16）：公开因子库在美股 IC 0.01-0.035；LLM 挖掘论文 IC 0.045-0.085（多为 A 股或不公开表达式）；
  隔夜/日内分解（Lou-Polk-Skouras）隔夜十分位 alpha 3.47%/月；空头 days-to-cover 多空 1.2%/月（FINRA 免费）；
  SPY 日内动量样本外 R² 最高 2%；GTJA-191 在标普 500 存活 17 个；内部人交易可为异象择时。

## 2. 工作流（按优先级；工时 = 执行代理工作日）

| # | 工作流 | 内容 | 依赖 | 工时 |
|---|---|---|---|---|
| W0 | 补完 Step 16 调研 | 只做未完成的 5 项：跳空反转/波动压缩（找有数字的一手来源，找不到就判为仓库内假设）、散户关注度（Google Trends 一手论文）、LLM 读 EDGAR/财报电话会（Lopez-Lira & Tang；Kim, Muhn, Nikolaev；直接抓 arXiv/SSRN PDF）、已发表异象的当前样本外收益（openassetpricing.com 当前版本数据）、2025-2026 纯价量美股 IC > 0.05 的论文；预算 15 次联网调用、≤ 8 万 token；追加到 step16 来源卡与报告 | 无 | 0.5 |
| W1 | 指标层做扎实（Reversal Trend） | 向用户要原版截图与更新版 Pine；`reversal_trend_hourly.py` 泛化为 `--timeframe {1m,2m,5m,15m,30m,1h}`（聚合函数复用 hourly.py）；部件消融（趋势层/武装态/MACD 触发/冷却）；阈值在训练窗学习、留出验证；1m/5m ETF 单元同时跑"收盘前平仓"与"允许隔夜"两种（隔夜溢价证据）；DSR 计全部周期 | 用户截图（可并行） | 2 |
| W2 | Step 14 通用运行器 | BarSource / SignalEngine / 幂等 BarCycleRunner；日线周期变薄包装；小时线候选接观察模式 | 无 | 2 |
| W3a | 选股层：GTJA-17 | 补齐 15 个未实现 id、重建、重筛、并入筛选集单元 | 无（零新数据） | 1 |
| W3b | 选股层：隔夜/日内分解 + 空头利息 | 日频特征 `ret_overnight_*`/`ret_intraday_*`（零新数据）与 FINRA days-to-cover（先登记能力）；进筛选器；隔夜择时门作为规则孪生 | 能力登记 | 1 |
| W3c | 选股层：内部人 PIT 包（Step 15B） | SEC 季度数据集 + EDGAR 日更；因子与"动量确认信号"两种用法 | 能力登记 | 2 |
| W4a | 组合层：两阶段与动态刷新 | 规则前 100 → 筛选集排序前 50；每季重筛的动态因子集 | W3a | 1 |
| W4m | 组合层：ML 方法按层匹配（用户 2026-09-11 指出方法过于单一） | 每个数据层预注册一个小的"方法 × 目标"网格，而不是一律 LightGBM 回归 + 等权前 50：选股层 = learning-to-rank（LambdaRank）/ 分位数或分类目标 / 残差化与"仅隔夜"标签 / 岭回归基线；信号层（Reversal Trend）= 元标签（ML 决定是否接受并按概率给规则信号加仓位）；择时层 = 体制分类（HMM 或梯度提升，输入波动、宽度、隔夜/日内比、新闻事件计数）门控规则组合；组合构造 = 概率加权、波动目标、前 k 内风险平价；每种方法一个占位；先在已有特征上跑，不等新数据 | W3a | 2 |
| W4b | 组合层：LLM 的多种角色 | 因子生成器（FactorMiner/AlphaMemo 式：LLM 提公式（受限算子 DSL）→ 筛选器评估 → 经验记忆 → 迭代；预注册通过标准 | LLM 端点恢复 | 2 |
| W4c | L 轨恢复 | 批量事件抽取 → 4.3 特征 → 4.4 评估 → 前向流水线 | LLM 端点恢复 | 2 |
| W5 | 组合实验 | 动量池 × Reversal Trend 入场（随机入场占位）——只在 W1 证明指标有信息之后 | W1, W2 | 0.5 |
| W6 | 后备（Step 16 低优先） | 13F 倾斜、Kyle-lambda 列、Chen-Zimmermann 当前样本异象收益（先抓取）、LLM 读 EDGAR 情绪、散户关注度 | — | 按需 |

## 3. 顺序与额度

- 周日 00:00 UTC 重置后第 1 天：W0（半天，一个代理）+ W3a 启动；第 1-2 天：W1 与 W3a 并行（两个代理）；W3b 的零数据部分（隔夜/日内特征）由协调者用后台链完成。
- 第 3-4 天：W2；W3c 开始。
- 第 5 天起：W4a、W5；端点恢复后插入 W4b/W4c。
- 每个工作流一个新代理、一份自包含简报；评估全部后台跑；每天一次汇总汇报，末尾附"接下来做什么、为什么"。

## 4. 需要用户的输入

1. 原版 Reversal Trend 指标截图（1h，NVDA/TSLA/SPY，2-3 个月，日期可见）与更新版 Pine——用户表示稍后提供。
2. 架构与优先级：已认可（2026-09-11）。
3. 重置时间：北京时间周日 08:00。
4. 行情：SIP（已确认）。
