# Review：2026-09-01 kernel 搜索工作的自查

日期：`2026-09-01`
范围：提交 `38aeb35`..`HEAD`（P1a / D2 / D3 / P1b / P2a / P2b / F + 保留策略）
触发原因：用户指出"以前不是按固定参数做的"，并要求排查同类、潜在、以及做错的问题。

---

## 0. 结论先说

用户的怀疑是对的。本轮引入了 **6 个方法论缺陷**，其中 4 个会**放松门槛或高估成绩**——
也就是危险的那个方向。全部已修，但**根因值得单独记下来**：

> **反复出现的错误模式：另起炉灶造新机制，而不先查仓库已有的实现。**

`campaign.py` 早有有效 N 的五道防线，`router_common.py` 早有按折重选参数，
`optimizers/` 早有搜索包。kernel 三次绕开了它们，三次都重新引入了那些防线本来挡住的问题。

---

## 1. 已修复的缺陷

### 1.1 固定参数取代按折重选（用户发现）

kernel 在 2016-2021 上选定**一组**参数，拿去 2022-2026 跑。
而 `router_common.walk_forward_router:465` **一直是每折重选**的。

两个后果：选择环节永远看不到近期 regime；验证的对象不是实际会部署的东西
（实际部署的是"定期重新优化的流程"）。

修复：`kernel/nested_walk_forward.py`，恢复按折重选，**同时保留旧实现缺的
purge+embargo**（`router_common` 的训练期与测试期直接相接）。

实测价值：真实 SIP 日线上，各折选出的 `lookback` 是 210/110/110/130/130，
churn 0.50。**参数是会动的**——固定设计只会报一个数，把这件事藏起来。

### 1.2 DSR 的有效 N 被做小（放松方向）

`layered_search` 只对**存活的精英**聚类，并按该数收 DSR 的费：
60 个候选筛成 5 个、聚成 3 类，就按 3 次试验计价。

`campaign.py:_effective_trial_clustering_blockers` 的 docstring 逐字点名禁止这件事
（"clustering a hand-picked subset"）。**这个缺陷我在 P1a 计划评审时识别过、
在 campaign 路径里修过，然后在 kernel 路径里又犯了一遍。**

修复：聚类**全部已评估候选**并取较大者，只可能收紧。参考运行的试验计数 3 → 6。

### 1.3 选择层看到的是改名后的 OOS（执行代理自查发现）

`Candidate.development_fold_returns` 是把拼接 OOS 流按各折**测试窗口**切片得到的——
展平后与 `oos_return_stream` **逐元素相同（1240/1240）**。
所以"Layer 1/2 只能读它"只是类型层面的隔离，**选择实际上在 OOS 上做**。

字段名本身就是成因。修复：改名为 `oos_fold_returns`（它一直是这个），
新增真正的 `development_returns` = **第一个测试窗口之前**那段，
`expand_mechanism` 在两者有任何日期交集时直接报错。

注意：**逐折训练窗口是错误的替代方案**——锚定扩展窗口下 fold5 的训练期包含
fold1-4 的测试年，实测重叠 1039 天。

### 1.4 未来函数检测器有探针盲区（对抗测试发现）

`assert_causal_transform` 原本固定探测 `[40%, 90%]` 区间。
用**绝对日期锚定**、落在最后 10% 的泄露可以完全穿过——**把探针数加到 40 个也没用**，
因为多出来的探针落在同一个窗口里。盲区正好是最近的数据，也是意外泄露最常发生的地方。

修复：探针范围改为从变换自身预热期末尾到"仍有未来可截断"的最后一个位置。
修复后头部/尾部泄露、居中均值、全样本 z-score 全部拦截，
因果滞后均值 / 因果 EWM / 滚动 std 无误报。

### 1.5 审计指标口径矛盾

报告并排写 `raw_candidate_count: 65` 和 `breadth_ratio: 0.5`，
但 0.5 的分母是 6 个精英，不是 65。照字面读会算出 32 个有效试验，实际是 3 个。
`breadth_ratio` 的**唯一用途就是审计虚假宽度**。

修复：两个分母都明确命名（`breadth_ratio` / `breadth_ratio_vs_discovery`）。

### 1.6 硬编码门槛绕过预注册

`campaign.py` 的阈值是**哈希封存合同的字段**，`mechanism_eval.py` 把一组数字写死在
module dict 里。任何人看到裁定后都能改它。

已做：裁定携带 `gates_provenance`，`MIN_DSR_STREAM_ROWS` 改为从 campaign 导入（消除双份常量）。

随后**已根治**：门槛改为必须来自 git 已提交的合同，见 §3.1。

---

## 2. 独立验证通过的部分

不是"代理说通过"，是我自己重跑过的：

| 检验 | 结果 |
|---|---|
| VOL02 数值回归（IEX 输入喂进新 harness） | 17 位浮点完全复现 `0.35248398502299755` |
| 聚类正确性 | 3机制×10变体 → N=3；12 个独立 → N=12 |
| 嵌套前推逐折隔离 | 投毒 `test_start_k` 之后**全部**数据，5/5 折选择与分数逐位不变 |
| GP 的 16 个算子因果性 | 全部 causal，逐个单独过 `assert_causal_transform` |
| GP 的 8 个终端 | 全部**价格尺度不变**（后向复权无法污染） |
| 深度/节点限制 | 构造时强制：depth 5 与 15 节点均被拒 |
| 未来函数防护是否"活的" | 注入 `shift(-1)` / 居中均值 / 全样本 z-score，**三种全部拦截** |
| Drive 归档完整性 | 1962/1962 文件 md5 一致 |
| SIP 日线连续性 | QQQ/SPY/TQQQ 各 2680 条，最大间隔 4 天，零重复零缺失 |

---

## 3. 未修复 / 需要决策的问题

### 3.1 kernel 整体在预注册体系之外（**已修复，2026-09-01**）

原问题：campaign 路径的保护是**合同 + 哈希封存 + 五道聚类防线**，
kernel 路径的保护只是**代码里的常量**。§1.2 和 §1.6 都是这个结构性差距的症状。

修复方式参照主流做法——回测过拟合文献（Bailey & López de Prado 的 DSR、
Harvey & Liu 的因子识别协议）的共同要求是**先声明再看结果**；
工程上对应的是 config-as-code + 哈希。这里落地为：

`open_composer/research/kernel/gate_contract.py` 要求门槛来自一个 JSON 文件，且：
- 必须**被 git 跟踪**；
- 必须**没有未提交的改动**；
- 裁定记录该文件的 **git blob SHA-1 与内容 SHA-256**。

看到结果之后改阈值，要么在加载时因工作区脏被拒，要么留下一条可审查的 commit diff。
这挡不住蓄意作弊（没有任何机制能），但**让它必须留下痕迹**——预注册在别处买到的也正是这个。

新增 `CandidateVerdict.promotion_eligible`：**通过全部门槛是必要不充分条件**，
阈值非预注册时恒为 `False`。实测判别性：

```
同一候选、同样数值、三种来源:
  默认门槛          all_gates_pass=True  promotion_eligible=False
  预注册合同        all_gates_pass=True  promotion_eligible=True
  裸 dict(同样数值)  all_gates_pass=True  promotion_eligible=False
```

**把同样的数字内联传进去拿不到晋级资格。** 合同：
`config/promotion/kernel-paper-tier-gates.json`（先于读取它的代码单独提交，
阈值沿用 Work Item A 的重校准值，未做任何改动）。

### 3.2 抓取续传有静默数据缺口的隐患（**已修复**）

`BATCH_SIZE` 中途从 40 改成 12，续传只认分片号：

```
整年分片 316（旧run最后）: VSS .. VUSE      <- 字母表 94%
月分片   317（新run第一）: EP.PRC .. EPM    <- 字母表 28%
```

同一个分片号指向完全不同的标的。实测本次**没有丢数据**——新编号起点恰好更靠前，
盖住了旧范围。**但这是运气。** 批大小往大改就是静默缺口。

修复：`data/sip/{kind}/_LAYOUT.json` 记录 `batch_size` 与宇宙大小，
`assert_resumable_layout()` 在续传前校验，不一致即致命退出。
**刻意不做自动迁移**——反推"哪个已有分片装了哪些标的"正是会产生静默缺口的那种猜测。

已为两个现有归档补写布局文件。实测：minute（batch_size=12，与当前一致）放行，
所以正在跑的回补重启不受影响；daily（batch_size=40）被正确拒绝。

### 3.3 GP 的结构演化仍是全局的（**已修复，并因此暴露一个更重要的结果**）

原问题：`expression_tree_search` 在开发分区（2016-2021）上演化出精英池，
之后只有"在池子里选哪一个"是逐折的。**表达式的结构仍冻结在 2016-2021 的数据上**——
就是 §1.1 的问题上移了一层。

修复：`run_nested_expression_gp_search` 逐折**重新演化**，
每折只用 `[train_start_k, train_end_k]`，折种子由基种子确定性派生。
关键实现细节：**切的是 OHLCV frame 本身**，不只是日期边界——
因为 `validate_expression_is_causal` 直接读 frame 参数，不看边界。

独立验证（逐折只毒该折自己的测试窗口）：

```
fold1: 只毒 2023-01-02..2023-12-29 -> 演化报告逐字段相同=True  精英相同=True
fold2: 只毒 2024-01-01..2024-12-31 -> 演化报告逐字段相同=True  精英相同=True
fold3: 只毒 2025-01-01..2025-03-14 -> 演化报告逐字段相同=True  精英相同=True
```

**这个修复暴露出的结果比修复本身重要。** 逐折演化后：

```
fold1 test 2022: gt(roll_mean_5(gt(oc_gap, -0.02)), roll_std_10(-0.2))
fold2 test 2023: add(0.1, neg(ret5))
fold3 test 2024: max2(max2(volchg5, abs(oc_gap)), neg(sign(ret20)))
fold4 test 2025: roll_std_10(gt(max2(0.1, hl_range), roll_mean_5(-0.1)))
fold5 test 2026: gt(mul(add(ret20, ret5), neg(-0.2)), -0.05)

churn_rate = 1.00  (5/5 结构全不同)
```

**每一年演化出的获胜公式毫无共同结构，这是 GP 在拟合噪声的直接证据。**
旧的全局演化把这件事藏住了——它报 churn 0.25，只是因为池子被冻结、没得可换。

试验计量同步收紧：633 个去重表达式 → `search_effective_n=168` → DSR 按 168 收费
（旧口径 44）。DSR 概率 0.034，8 门槛过 3。

### 3.4 "模仿现金"的退化候选（**已修复**）

裸夏普奖励低方差：一直持有国债 = 小正漂移 ÷ near-zero 分母 = 夏普爆表。
GP 很容易演化出常数信号的公式，正是这个形态。P2b 在自己的准入闸门修了，
**但 `development_quality` 本身没改，其他调用方全部暴露**——所以下限应该放在共享函数里。

修复：`MIN_DEVELOPMENT_ANNUALIZED_VOLATILITY = 0.01`，低于此年化波动直接 `-inf`。
1% 这个位置是刻意选的——远低于任何可交易策略，远高于现金。实测判别性：

```
纯现金模仿        年化波动  0.19%   quality = -inf   拒绝
防御型(2成权益)    年化波动  3.52%   quality = 1.437  保留
权益型            年化波动 17.51%   quality = 0.349  保留
```

**没有误伤防御型策略**——"回撤时切到国债"恰恰是本项目要找的东西，这个区分是关键。

### 3.5 `_qqq_capture_gate_passes` 的免费通行（**已修复**）

与 QQQ 相关性 ≤0.3 时，两个 capture 门槛直接通过。理由站得住（不相关时捕获率没有意义），
但后果是**噪声策略白拿 8 个门槛里的 2 个**。不足以单独通过（另外 6 个还在）。

**归属澄清**：来自更早的 `a4a5b07`（Work Item A3/A4），**不是本轮引入**。

危害重估：剩下 6 个门槛仍包含 `cagr_excess_qqq`（需超 QQQ 5 个百分点）与
`sharpe_excess_bil > 1.0`，所以免费通行没有最初担心的那么可利用。
真正的问题是**报告把它算成 8/8 通过，读的人看不出证据基础更窄**。

修复：裁定新增 `gates_not_applicable` 与 `evaluated_gate_count`。
正交候选的报告现在是"8 项结果，其中 6 项真正被检验"，而不是"8/8 通过"。
**按构造通过不是证据**，报告不该读起来比证据更强。

### 3.6 归置与文档（**已处理，但改了修法**）

- `parameter_search.py` 的归置：**重新评估后没有移动文件**。
  `optimizers/` 服务的是 StrategySpec 层（配合 `parameter_sweep.py`，写产物、过迭代闸门），
  `kernel/parameter_search.py` 服务的是 kernel 层（纯参数字典，无 spec 无产物）。
  把 kernel 模块搬进 `optimizers/` 只会加深耦合混淆。
  真正的病根是**可发现性**——有人找参数搜索时先撞见 `optimizers/`，不知道 kernel 里那个，
  于是重复造轮子。所以改为**双向交叉引用**：两处文档都写明分层差异和"不要加第三个"。
- `spec.data.source` 是 `Literal["sample","alpaca","longbridge"]` 且在禁改文件里，
  `sip_parquet` 无法成为合法取值。溯源走 `frame.attrs` + `DataAssumptions.source`，
  **没有误报**，但运行时 spec 块叫不出它读的归档名字。

### 3.7 执行代理报告与产物不一致
P2b 代理报告 `rejected as degenerate: 47`，产物 JSON 是 `108`。产物为准。
记录于此是因为**这正是"不核对就采信"会出问题的地方**。

---

## 4. 本轮所有搜索的实际结果

**全部为负。没有任何候选通过全部 8 个门槛。**

| 搜索 | 规模 | 试验计数 | 门槛结果 |
|---|---|---|---|
| P2a 参数搜索（嵌套） | 40 网格 + 25 变异 | dsr_trial_count=6 | 8 过 3，DSR 0.048 |
| P2b 表达式树 GP（嵌套） | 136 个表达式 | dsr_trial_count=44 | 8 过 3，DSR 0.094 |

计划 §6.2 明确禁止因为搜索没找到东西就放松门槛。**门槛一分没动。**

有效 alpha 极度稀疏是文献共识，两轮负结果完全在预期内，不构成放宽的理由。

---

## 5. 给下一轮的硬规则

1. **动手写新机制前，先在仓库里搜同名/同义的既有实现。** 本轮 3 次教训都来自跳过这一步。
2. **任何改动都要问"方向是收紧还是放松"**，放松方向的改动需要显式论证。
3. **防护必须证明是活的**——注入一个它该拦的东西，看它拦不拦。
   `rejected_lookahead_count: 0` 单独看无法区分"没有泄露"和"防护是死的"。
4. **代理报告的每个数字都要与产物核对**（§3.7）。
5. **命名即契约**：`development_fold_returns` 里装 OOS 数据，是 §1.3 的直接成因。
