# 近期盈利目标与自主研究修复 — 2026-09-21

依据：用户本轮委托；`docs/research-mission.zh.md`；本轮联网情报与官方文档。任务不以代码数量或实验次数验收，而以消除实际阻塞和获得可证伪结论验收。

| 节点 | 目标与依赖 | 负责人/独占写入 | 验收与停止条件 |
|---|---|---|---|
| A 情报 | 无依赖；核对现成策略/实现/复现与当前数据适配 | research scout；本轮 intel 和 source cards | 原文可追溯，列出反证和下一最小测试；不训练、不回测 |
| B 数据恢复 | 查官方 DuckDB/Qlib 实现后，修 Alpha158 合并内存和过期 cron | data implementer；Alpha158 builder、wrapper、对应测试和运维记录 | 分片重启/结果一致/内存有界，旧任务精准移除；不触碰 paper cron |
| C 共同使命与入口 | 官方 Codex/Claude 记忆及 hooks 文档 → 共用使命、中文提醒、方向检查 | 主会话；规则、hook、方向校验/CLI | 缺来源/过期/哈希不匹配不能过新版入口；历史冻结评估不重定义 |
| D 自主调度 | A/C + 本地预算来源 → 单引擎控制器、待办和空跑 | 主会话；调度模块、配置、文档 | 未配置预算不派单；锁、超时、429 暂停、完成任务不重复；启用依赖明确预算 |
| E 集成 | B/C/D → 独立反例审查、修复、全量校验 | 主会话整合，独立 reviewer 只读 | ruff format/check、pytest、repo check 和 agent parity；记录失败与外部依赖 |

不把本轮工程修复当成策略收益证据。新研究的收益目标/回撤容忍度等用户补充前，沿用每个旧候选已经冻结的合同；不得偷偷改分数或重跑已看过的 OOS。用户本机模型的用途、来源和接口待明确，模型未接入不阻止其他独立工作。
