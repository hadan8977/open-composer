# Copy selected sections into ~/.codex/config.toml or a trusted project .codex/config.toml.

[mcp_servers.openaiDeveloperDocs]
url = "https://developers.openai.com/mcp"
enabled = true

# Optional model provider example. Open Composer accepts trusted OpenAI-compatible
# Responses API gateways through OPENAI_BASE_URL or this Codex provider base_url.
# model = "gpt-5.5"
# model_provider = "trusted_gateway"
#
# [model_providers.trusted_gateway]
# base_url = "https://your-openai-compatible-gateway.example/v1"
# env_key = "OPENAI_API_KEY"
# wire_api = "responses"

# Add data, browser, or GitHub MCP servers later as the workflow matures.
