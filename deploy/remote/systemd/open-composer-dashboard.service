[Unit]
Description=Open Composer Dashboard
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=opencomposer
Group=opencomposer
WorkingDirectory=/srv/open-composer/repo
EnvironmentFile=/srv/open-composer/repo/.env
ExecStart=/usr/bin/env uv run oc dashboard serve --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=5
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ReadWritePaths=/srv/open-composer/repo

[Install]
WantedBy=multi-user.target
