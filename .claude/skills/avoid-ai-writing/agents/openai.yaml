interface:
  display_name: "Avoid AI Writing"
  short_description: "Detailed prose audit with source fidelity"
  default_prompt: "Use $avoid-ai-writing to audit or rewrite this text with its detailed rulebook while preserving voice, facts, and protected content."
policy:
  allow_implicit_invocation: false
